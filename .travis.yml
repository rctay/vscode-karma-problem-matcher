language: node_js
node_js:
  - "8"
cache: yarn
before_script: npm run compile
jobs:
  include:
    - stage: Compile
      script: npm run compile
    - stage: Test
      script:
        - npm run test:js
        - npm run test:only
